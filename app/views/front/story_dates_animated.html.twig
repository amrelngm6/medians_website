
                {% for story_date in story_dates %}
                <div class="m-2 mx-1  ">
                    <span class="mx-auto relative w-20 text-center block bg-white border-2 border-purple-600 my-4 rounded-lg py-1 px-2 font-bold purple-color">{{story_date.title}}</span>
                    <div class="p-2 py-6 text-center rounded-2xl bg-white">
                        <span class="font-bold ">{{story_date.content.title}}</span>
                        <p class="my-2 text-sm max-h-32 custom-scroll overflow-auto">{{story_date.content.content}}</p>
                    </div>
                </div>
                {% endfor %}